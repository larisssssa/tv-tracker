# TV Tracker — frontend

React + TypeScript + Vite app for TV Tracker.

See the [root README](../README.md) for architecture, the full page/API
reference, and run instructions. Quick start from this directory:

```bash
npm install
npm run dev
```

Linting uses [Oxlint](https://oxc.rs) (`npm run lint`), not ESLint.
